---
title: "API reference"
description: "Fundamentals and routing overview for the unified DimiLinks API."
icon: "terminal"
---

## Request URL

All public API requests use `api-direct.dimilinks.com`. The recommended request URL for model APIs is `https://api-direct.dimilinks.com/v1`, and all authenticated model endpoints share the same Bearer key.

| Client type | Base URL to configure | Behavior |
| --- | --- | --- |
| OpenAI-compatible SDK or generic HTTP client | `https://api-direct.dimilinks.com/v1` | Append routes such as `/chat/completions`, `/models`, and `/images/*` directly |
| Official Anthropic SDK or Claude Code | `https://api-direct.dimilinks.com/` | The client appends `/v1/messages`; do not include `/v1` twice |

See [Authentication](/en/api-reference/authentication) for details.

## Route overview

| Method | Full URL | Description |
| --- | --- | --- |
| `GET` | `https://api-direct.dimilinks.com/v1/models` | List the models available to the current key |
| `POST` | `https://api-direct.dimilinks.com/v1/chat/completions` | OpenAI-compatible chat for GPT; also supports Claude and other text models through the OpenAI protocol |
| `POST` | `https://api-direct.dimilinks.com/v1/messages` | Native Anthropic protocol; recommended for the Claude SDK and Claude Code |
| `POST` | `https://api-direct.dimilinks.com/v1/images/generations` | Text-to-image generation with JSON reference-image extensions |
| `POST` | `https://api-direct.dimilinks.com/v1/images/edits` | Multipart image-to-image generation and editing |
| `POST` | `https://api-direct.dimilinks.com/v1/videos/generations` | Text-to-video and reference-image-to-video generation |
| `GET` | `https://api-direct.dimilinks.com/v1/tasks/{task_id}` | Shared image and video task retrieval |
| `GET` | `https://api-direct.dimilinks.com/v1/videos/{task_id}/content` | Direct video task result (authentication required) |
| `GET` | `https://api-direct.dimilinks.com/p/img/{task_id}/{idx}` | Signed image URL (no Bearer token required; the signature is embedded in the URL) |

## Protocol conventions

- Text chat: compatible with the official OpenAI and Anthropic SDKs; point the SDK's base URL to DimiLinks.
- Images: responses follow OpenAI Images conventions. `data[].url` may be a relative path, an HTTPS URL, or a Data URL.
- Videos: asynchronous by default. Submission returns a `task_id` immediately; poll `/v1/tasks/{task_id}` for progress.

## Recommended asynchronous workflow

```text
POST https://api-direct.dimilinks.com/v1/images/generations?async=true   # Submit an image task
POST https://api-direct.dimilinks.com/v1/videos/generations              # Submit a video task (always asynchronous)
GET  https://api-direct.dimilinks.com/v1/tasks/{task_id}                 # Shared task retrieval
```

Image and video generation times vary. The asynchronous workflow avoids long-lived connections, gateway timeouts, and tasks being lost when a page closes.
